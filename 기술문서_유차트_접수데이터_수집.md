# 유차트(Dr.BIT, MS-SQL)에서 접수 데이터를 가져오는 방법 — 기술 정리

작성일 2026-09-15 · 대상: 유차트 접수 정보를 외부 시스템(여기서는 "반듯한 동선관리" 웹앱)으로 넘기려는 개발자
구현체: `유차트연동/agent.py`(Python 3.12, pyodbc + firebase-admin). 아래는 그 구현에서 확인된 사실과 설계를 정리한 것입니다.

> 보안 원칙: 유차트 DB에는 **읽기 전용 계정으로 SELECT만** 합니다. 어떤 경우에도 유차트 데이터를 쓰거나 바꾸지 않습니다. 비밀번호·서비스 계정 키는 이 문서에 없습니다(별도 전달, 저장소에 올리지 않음).

---

## 1. 환경

| 항목 | 값 | 확인 방법 |
|---|---|---|
| EMR | 유차트 = 비트컴퓨터 Dr.BIT 계열(옛 비트플러스 웹과 같은 컬럼 명명 체계) | 설치 폴더 `c:\drbit50` |
| DB 서버 | `192.168.50.100`, TCP **1433**, 기본 인스턴스 | 유차트 환경설정 화면(DataBase Server) |
| 서버 OS/DB | Windows 11 Pro, **SQL Server 2025 Express** (호스트명 DESKTOP-DILI0NC) | `SELECT @@VERSION` |
| 데이터베이스 | **`DrBITPACK`** | `c:\drbit50\SQL` 벤더 스크립트 |
| 인증 | SQL 로그인(혼합 모드) | SSMS |
| 클라이언트 드라이버 | Windows 내장 `SQL Server` ODBC 드라이버로 충분(`ODBC Driver 17/18 for SQL Server`도 가능) | — |

## 2. 읽기 전용 계정 만들기 (SSMS, 관리자 권한으로 1회)

```sql
CREATE LOGIN dongseon_ro WITH PASSWORD = '강한비밀번호';
USE [DrBITPACK];
CREATE USER dongseon_ro FOR LOGIN dongseon_ro;
ALTER ROLE db_datareader ADD MEMBER dongseon_ro;
```

- `db_datareader`만 부여합니다. INSERT/UPDATE/DELETE/EXEC 권한이 없어 코드에 버그가 있어도 DB는 바뀌지 않습니다.
- 벤더 계정(sa 등)은 절대 그대로 쓰지 않습니다.
- 사용자 이름을 잘못 만들었을 때: `ALTER USER 잘못된이름 WITH NAME = dongseon_ro;`

## 3. 관련 테이블과 컬럼 (실데이터로 확인)

| 테이블 | 역할 | 주요 컬럼 |
|---|---|---|
| `OcmInf` | **접수(내원) 1건 = 1행** | `OcmNum` char(10) 접수번호(증가, **우측 정렬 패딩 → RTRIM 필요**) · `OcmAcpDtm` char(12) `'YYYYMMDDHHMM'` 접수(도착) 일시 · `OcmChtNum` char(14) 차트번호(=병록번호) · `OcmDtrCod` 진료의 코드 · `OcmComStt` char(2) 상태 · `OcmDepCod` 진료과 · `OcmUpdDtm` 갱신 일시(**신뢰 불가**, 변경 감지에 쓰지 말 것) |
| `PbsInf` | 환자 기본정보 | `PbsChtNum` 차트번호 · `PbsPatNam` 이름 · `PbsResNum` varchar(13) 주민번호 13자리 붙여쓰기 · `PbsCelPhn` 휴대폰 · `PbsPhnNum` 전화 · `PbsSexTyp` |
| `UidMst` | 사용자(진료의) | `UidCod` 코드(예: `OS`) · `UidNam` 이름 |
| `RsvInf` | 예약 | `RsvOcmNum` 접수번호(예약이 접수행과 연결됨) · `RsvDtm` 예약 일시 · `RsvSts` |
| `DtlMst` | 공통 코드표 | `DtlTblCod='COMSTT'` 로 접수 상태 코드 36개 조회 가능 |

조인: `OcmInf.OcmChtNum = PbsInf.PbsChtNum`, `OcmInf.OcmDtrCod = UidMst.UidCod`, `RsvInf.RsvOcmNum = OcmInf.OcmNum`.

### 접수 상태 코드 `OcmComStt` (DtlMst COMSTT 기준, 실관찰로 확인)

| 코드 | 의미 | 접수로 볼지 |
|---|---|---|
| `WN` | Registration Waiting — 접수 대기(도착) | **예** |
| `NN` | Nurse Registration | 예 |
| `WR` / `NR` | Reserve Waiting — 예약만 잡힘, **아직 미도착** | 아니오 |
| `TN` / `FN` | OCS Complete / Finish Consult — 진료 완료 | 예(이미 접수된 환자) |
| `PN` | Payment Complete — 수납 완료 | 예 |
| `HN` | OCS Retain — 보류 | 예 |
| `CN` | Cancel Registration — 접수 취소 | 아니오 |
| `CR` | Cancel Reserve — 예약 취소 | 아니오 |
| `O*`, `V*` | 입원 관련 | 아니오 |
| `WC/HC/TC/FC/PC` | 위 상태의 변형(같은 계열) | 예 |

**중요한 동작 특성**
- 예약 환자는 예약을 잡는 순간 이미 `OcmInf` 행이 생깁니다(상태 `WR`). 도착 처리하면 같은 행의 `OcmAcpDtm`이 예약 시각 → **실제 도착 시각으로 갱신**되고 상태가 `WR → WN`(→ `TN` → `PN`)으로 바뀝니다. 따라서 "새 행이 생겼는가"로는 접수를 잡을 수 없고, **상태가 접수 계열로 바뀌는 순간**을 잡아야 합니다.
- 테스트 접수를 취소하면 행이 남고 상태만 `CN`이 됩니다.
- `WN`이면서 `OcmAcpDtm`이 미래(오늘 오후, 며칠 뒤)인 행이 있습니다(물리치료 등 사전 등록). 그 시각이 되기 전에는 보내지 않습니다.

## 4. 조회 쿼리 (오늘 접수분 전체, 잠금 없음)

```sql
SELECT RTRIM(o.OcmNum) AS k, RTRIM(o.OcmComStt) AS stt,
       p.PbsPatNam AS name, o.OcmChtNum AS mrn, p.PbsResNum AS rrn, o.OcmAcpDtm AS recv,
       COALESCE(NULLIF(RTRIM(p.PbsCelPhn),''), p.PbsPhnNum) AS tel,
       u.UidNam AS dr,
       CASE WHEN r.RsvOcmNum IS NULL THEN 'N' ELSE 'Y' END AS rsv, r.RsvDtm AS rsvdtm
FROM OcmInf o WITH (NOLOCK)
LEFT JOIN PbsInf p WITH (NOLOCK) ON p.PbsChtNum = o.OcmChtNum
LEFT JOIN UidMst u WITH (NOLOCK) ON u.UidCod = o.OcmDtrCod
LEFT JOIN RsvInf r WITH (NOLOCK) ON r.RsvOcmNum = o.OcmNum
WHERE LEFT(o.OcmAcpDtm, 8) = CONVERT(varchar(8), GETDATE(), 112)
ORDER BY o.OcmAcpDtm
```

- 하루 수십 행 수준이라 4초마다 전체를 읽어도 부하가 없습니다. `NOLOCK` + `READ UNCOMMITTED`로 유차트 트랜잭션을 기다리게 하지 않습니다.
- 이 SQL Server에서 `STRING_AGG`는 구문 오류가 났습니다(호환성 수준). 필요하면 애플리케이션에서 집계하세요.

## 5. 연결 방식 (pyodbc 예시)

```python
import pyodbc
CONN = ("DRIVER={SQL Server};SERVER=192.168.50.100;DATABASE=DrBITPACK;"
        "UID=dongseon_ro;PWD=<비밀번호>;")
cn = pyodbc.connect(CONN, timeout=10, readonly=True, autocommit=True)
cur = cn.cursor()
cur.execute("SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED")
cur.execute("SET LOCK_TIMEOUT 3000")        # 잠금 대기 최대 3초 → 유차트 작업을 막지 않음
cur.execute(QUERY)
rows = [dict(zip([c[0] for c in cur.description], r)) for r in cur.fetchall()]
cn.close()                                   # 폴링마다 열고 닫음(연결 유지 안 함)
```

추가 안전장치(권장): 실행 전에 SQL을 검사해 `SELECT` 한 문장만 허용하고 `;`가 있거나 `INSERT|UPDATE|DELETE|MERGE|DROP|ALTER|CREATE|TRUNCATE|EXEC|GRANT|REVOKE|INTO|BULK|OPENROWSET|xp_*|sp_*` 단어가 있으면 기동을 거부합니다(`agent.py`의 `assert_readonly_sql`).

## 6. "새 접수" 판정 알고리즘 (`decide`)

상태 파일 `state.json` = `{ "date": "YYYY-MM-DD", "seen": { 접수번호: 상태 }, "sent": [보낸 접수번호…] }`

매 폴링(4초):
1. 오늘 행 전체를 읽는다.
2. 날짜가 바뀌면 상태를 초기화한다.
3. 각 행에 대해 **상태 ∈ 접수 계열(`active_statuses`) 이고 아직 `sent`에 없으면**:
   - 첫 폴링(프로그램 시작 직후)이면 보내지 않고 `sent`에만 넣는다(재시작 시 오늘 접수분을 중복 전송하지 않기 위한 스냅샷). 옵션 `send_existing_on_start=true`면 전부 보낸다.
   - 접수 시각이 `현재 + lead_minutes(5분)` 보다 미래면 보류(사전 등록분 — 그 시각이 되면 보냄).
   - 그 외에는 전송하고 `sent`에 추가한다.
4. `seen[접수번호] = 상태` 갱신, 상태 저장.

이 방식은 예약(`WR`) → 도착(`WN`) 전환, 수동 접수, 취소 후 재접수 모두를 "상태가 처음으로 접수 계열이 된 순간" 한 번만 잡습니다. 실측으로 접수 후 1초 안에 감지됩니다.

## 7. 전달 데이터 형식 (동선관리 앱 기준)

행 → 페이로드 변환(`row_to_payload`):

```json
{ "name": "홍길동", "mrn": "12345", "rrn7": "900101-1", "recv": "09:31",
  "tel": "01012345678", "dr": "조한솔", "k": "w", "rsv": "09:20" }
```
- `mrn`: 차트번호에서 숫자만 · `rrn7`: 주민번호 13자리 → `YYMMDD-S`(앞 7자리만, 뒷자리는 절대 전달하지 않음) · `recv`: `OcmAcpDtm` → `HH:MM` · `tel`: 숫자만 · `rsv`: 예약이 있으면 `RsvDtm` → `HH:MM`(없으면 키 자체를 생략) · `k`: 항상 `'w'`.

저장 위치(Firestore): `patientboard_v3/우리의원/admits/{접수번호}` 문서에 위 필드 + `status:'new'`, `src:'uchart'`, `key`, `at`(ms), `date`. 문서가 이미 있으면 건너뜁니다(중복 방지). 접수 PC의 앱은 `at >= now-30분` 문서를 실시간 구독해 '환자 추가' 창을 채워 열고, 한 PC가 저장하면 `status:'done'`으로 바꿔 다른 PC의 창을 닫습니다. 2일 지난 문서는 에이전트가 삭제합니다. 다른 목적지(REST API 등)로 보낼 때는 `push()` 함수만 바꾸면 됩니다.

## 8. 운영 구조

- **에이전트는 유차트 서버 자체에서** 작업 스케줄러(부팅 시, SYSTEM 계정)로 실행됩니다(`C:\DongseonAgent`, 내장 파이썬 동봉이라 서버에 파이썬 설치 불필요). 설치기는 `유차트연동/서버패키지/install.cmd`.
- 여러 대에서 동시에 돌아도 접수가 두 번 올라가지 않도록, 각 인스턴스가 60초마다 `admits/_hb`에 생존 신호(`{hb, host, pid, started, prio}`)를 쓰고, **우선순위(`priority`)가 높은 인스턴스가 살아 있으면(3분 내 신호) 나머지는 대기 모드**로 전송을 멈춥니다(서버 10 > 관리 PC 1). 신호가 끊기면 자동으로 이어받습니다.
- 앱 상단에 🟢/🔴로 에이전트 상태가 표시됩니다.

## 9. 겪었던 함정 (재현하지 마세요)

| 증상 | 원인 | 해결 |
|---|---|---|
| 접수번호로 조회가 안 맞음 | `OcmNum`이 char(10) 우측 정렬(앞 공백) | `RTRIM` |
| 예약 환자 접수를 못 잡음 | 예약 시 이미 행 존재 | 새 행이 아니라 **상태 변화**로 판정 |
| 옛 접수가 재전송됨 | 재시작 시 `sent` 없음 | 시작 시 스냅샷(첫 폴링은 전송 안 함) |
| 미래 시각 접수가 바로 전송됨 | 사전 등록분 | `lead_minutes` 유예 |
| `OcmUpdDtm`으로 변경 감지 실패 | 갱신되지 않는 경우가 있음 | 사용하지 않음 |
| 한글 로그가 깨짐(Windows) | 콘솔 cp949 | `sys.stdout.reconfigure(encoding='utf-8')`, 로그 파일 utf-8 |
| 시작프로그램 `.vbs`가 실행 안 됨 | UTF-8로 저장된 vbs가 한글 경로를 깨뜨림 | vbs는 UTF-16, cmd는 ASCII+CRLF, 설치 경로는 영문(`C:\DongseonAgent`) |
| 서버에서 `requests library is not installed` | 파이썬 동봉 시 사용자 site-packages 누락 | idna·cffi·pycparser·typing_extensions·`_cffi_backend.pyd` 동봉 |

## 10. 처음 붙일 때 확인 순서

1. SSMS로 읽기 전용 로그인으로 접속해 `SELECT TOP 5 * FROM OcmInf ORDER BY OcmNum DESC` 가 되는지 확인.
2. `SELECT DtlCod, DtlNam FROM DtlMst WHERE DtlTblCod='COMSTT'` 로 상태 코드표를 한 번 출력해 둘 것(버전에 따라 코드가 다를 수 있음).
3. 4절 쿼리를 `--dry-run`(전송 없이 로그만)으로 돌리면서 실제 접수를 한 건 해 보고 로그에 "접수 전달" 줄이 1초 안에 찍히는지 확인.
4. 테스트 접수는 취소해도 행이 남으므로(`CN`) 이름을 "테스트"로 해서 구분.

문의: 조한솔(pinetreecho@gmail.com). 소스: 저장소 `유차트연동/` (`agent.py`, `config.example.json`, `test_agent_pure.py`, `설치안내.md`, `서버패키지/`).
